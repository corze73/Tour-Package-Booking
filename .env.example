# Environment Variables for Netlify + Neon
# Copy these to your Netlify dashboard under "Site settings" > "Environment variables"

# Database Configuration (Neon PostgreSQL)
DATABASE_URL=postgresql://username:password@your-neon-hostname.neon.tech/your-database-name?sslmode=require

# JWT Secret for Admin Authentication
JWT_SECRET=your-super-secure-jwt-secret-key-change-this-in-production

# Site Configuration
SITE_NAME=Adventure Tours Demo
SITE_URL=https://your-site-name.netlify.app
ADMIN_EMAIL=demo@adventuretours.com

# Optional: Email service configuration (for contact form notifications)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-email-password